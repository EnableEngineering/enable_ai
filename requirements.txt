# Core dependencies
requests>=2.25.0
openai>=1.0.0
langgraph>=0.2.0
python-dotenv>=0.19.0
langgraph-checkpoint>=1.0.0

# Optional: MCP Server support (install with pip install -e ".[mcp]")
# Note: MCP SDK may not be available on PyPI yet, install manually if needed

# Optional: Development dependencies (install with pip install -e ".[dev]")
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# flake8>=6.0.0
